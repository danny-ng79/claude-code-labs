{
  "name": "quiz-arena",
  "version": "1.0.0",
  "description": "Quiz Arena — a timed trivia game with streak scoring and leaderboard",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node tests/run.js",
    "lint": "npx eslint src/",
    "format": "npx prettier --write 'src/**/*.js' 'tests/**/*.js'"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
